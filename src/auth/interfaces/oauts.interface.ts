export interface IFacebookOAuth {
  email: string;
  accessToken: string;
}

export interface IGoogleOAuth {
  email: string;
  accessToken: string;
}

export interface IMicrosoftOAuth {
  email: string;
  accessToken: string;
}
